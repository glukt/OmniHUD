<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
	"-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
	"https://checkstyle.org/dtds/configuration_1_3.dtd">
<!--
	Derived from RuneLite's own checkstyle ruleset:
	https://github.com/runelite/runelite/blob/master/config/checkstyle/checkstyle.xml

	Two deliberate local changes vs upstream:
	  * the "indent must use tab characters" regex is dropped - this project is space
	    indented and re-indenting the tree is out of scope for the current polish wave;
	  * the SuppressionFilter points at checkstyle-suppressions.xml in this directory.

	The extra import / modifier / brace / line-length modules below are the mechanical
	categories a Plugin Hub review looks at. checkstyleMain is wired non-fatal
	(ignoreFailures = true, maxWarnings very high in build.gradle) - driving the
	violation count to zero is deliberately future work, not this wave.
-->
<module name="Checker">
	<property name="charset" value="UTF-8"/>

	<module name="TreeWalker">
		<!-- braces (RuneLite upstream) -->
		<module name="LeftCurly">
			<property name="option" value="nl"/>
		</module>
		<module name="RightCurly">
			<property name="option" value="alone"/>
		</module>
		<module name="NeedBraces">
			<property name="allowSingleLineStatement" value="true"/>
		</module>

		<!-- whitespace (RuneLite upstream) -->
		<module name="WhitespaceAround"/>
		<module name="WhitespaceAfter">
			<property name="tokens" value="COMMA"/>
		</module>
		<module name="RegexpSinglelineJava">
			<property name="format" value=".*\s+$"/>
			<property name="message" value="No trailing whitespace"/>
			<property name="ignoreComments" value="true"/>
		</module>

		<!-- imports -->
		<module name="AvoidStarImport"/>
		<module name="RedundantImport"/>
		<module name="UnusedImports">
			<property name="processJavadoc" value="true"/>
		</module>

		<!-- modifiers -->
		<module name="ModifierOrder"/>
		<module name="RedundantModifier"/>

		<!-- misc mechanical -->
		<module name="OneStatementPerLine"/>
		<module name="EmptyLineSeparator">
			<property name="allowNoEmptyLineBetweenFields" value="true"/>
			<property name="allowMultipleEmptyLines" value="true"/>
			<property name="allowMultipleEmptyLinesInsideClassMembers" value="true"/>
		</module>

		<module name="SuppressionCommentFilter"/>
	</module>

	<module name="LineLength">
		<property name="max" value="120"/>
		<property name="ignorePattern" value="^package |^import |https?://"/>
	</module>

	<module name="RegexpMultiline">
		<property name="format" value="else[ \t]*[\r\n]+[ \t]*if"/>
		<property name="message" value="Newline should not be between else and if"/>
	</module>

	<module name="SuppressionFilter">
		<property name="file" value="${config_loc}/checkstyle-suppressions.xml"/>
		<property name="optional" value="true"/>
	</module>
</module>
